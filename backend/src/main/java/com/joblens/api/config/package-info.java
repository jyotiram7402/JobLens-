/**
 * Application-wide Spring configuration: CORS, JPA auditing, and whatever
 * cross-cutting setup later steps require (security, in step 4).
 *
 * <p>Configuration that belongs to a single module lives with that module.
 * Only genuinely application-wide wiring belongs here.
 */
package com.joblens.api.config;
